#!/usr/bin/env bash

set -euo pipefail

# https://git-lfs.com/

TMP_DIR=$(mktemp -d)
ARCHIVE="$TMP_DIR/git-lfs.tar.gz"

echo "Downloading git-lfs... $TMP_DIR"

dra download -a -o "$ARCHIVE" git-lfs/git-lfs
tar xf "$ARCHIVE" --strip-components=1 -C "$TMP_DIR"
"$TMP_DIR"/install.sh --local
