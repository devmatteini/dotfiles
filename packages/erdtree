#!/usr/bin/env bash

# https://github.com/solidiquis/erdtree

dra download -s "erd-v{tag}-x86_64-unknown-linux-musl.tar.gz" -i -o ~/.local/bin/ solidiquis/erdtree

erd --completions bash > ~/.local/share/bash-completion/completions/erd
erd --completions zsh > ~/.local/share/zsh-completion/_erd
