#!/usr/bin/env bash

UBUNTU_VERSION=$(lsb_release -rs)

if [[ "$UBUNTU_VERSION" == "24.04" ]]; then
    sudo apt install -y clevis clevis-tpm2 clevis-luks clevis-initramfs initramfs-tools
else
    sudo apt install -y clevis clevis-tpm2 clevis-dracut clevis-luks
fi
